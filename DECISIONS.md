# Decisions log

One line per autonomous decision made overnight (2026-09-24), most recent last.
Timestamps are UTC.

- No `ANTHROPIC_API_KEY` or `SAM_API_KEY` in the environment; no `MAX_RUN_USD` set. Proceeding with SAM fixtures-only (never calling SAM live) and building/testing the LLM scoring path with mocked responses only; no live Anthropic spend is possible or attempted tonight.
- Checked `Kghaffari26/agents-core` (main @ `f79b6aa`, only branch): no commit yet has both `src/agents_core/guards.py` and `src/agents_core/registry.py` — the repo is still monorepo-shaped (`core/`, package name `agents-hub`, `[tool.uv] package = false`). Doing core-independent tasks (1, 2, 6-prep, 7, 8-prep) first and rechecking periodically, per instructions.
- Relevance pre-score (§5.3) keyword signals (title/description) use `profile.keywords` for both contracts and grants, not `profile.grant_keywords` (which the spec scopes to building Grants.gov search queries, §3.2). Grants still get NAICS/PSC = 0 as the spec expects, relying on the keyword and agency signals.
- Built the 40-item eval dataset/labels now (core-independent), but since task 4's LLM rubric scorer isn't built yet (blocked on agents-core), `evals/grants/run_evals.py` runs against a deterministic PROXY score (the §5.3 relevance pre-score + a regex clearance cap) instead of the real rubric, clearly marked `provisional_proxy`/`pending_task4` per check in the output JSON. Real Grants.gov records weren't available (network blocked, see above), so the dataset is hand-built to be realistic instead of pulled live.
- `.github/workflows/agent-grants.yml` calls `Kghaffari26/agents-core/.github/workflows/run-agent.yml@main` with `agent`, `max_run_usd`, `site_repo` exactly as instructed, plus `SAM_API_KEY` and `ANTHROPIC_API_KEY` secrets (the latter wasn't named explicitly but the run obviously needs it). As of agents-core @ `f79b6aa` that reusable workflow only declares `agent`/`args` inputs and runs the old monorepo `python -m core.runner`, so this file won't validate until agents-core's workflow is extended — documented in STATUS.md rather than modifying agents-core.
- `api.grants.gov` is blocked by this environment's egress policy (proxy returns 403 on CONNECT), so the "use Grants.gov live to record fixtures" instruction can't be carried out here. Built `tests/fixtures/grants/` by hand instead, matching the documented `search2`/`fetchOpportunity` response shapes from SPEC_GRANTS.md §3.2 as closely as possible. Flagged in STATUS.md for a real recording pass once network access allows it.
- 05:47-09:48 UTC: rechecked `Kghaffari26/agents-core` for an installable `src/agents_core/` layout every ~15 min for the full 4-hour window (per instructions). It never appeared — `main` stayed at `f79b6aa` the entire time, no other branches were ever created. Stopping the wait at the 4-hour cutoff (09:48 UTC) with tasks 3 and 4 undone, and writing the final STATUS.md instead of continuing to poll indefinitely.

## 2026-09-26 session (agents-core v0.1.0 wiring, fetchers, scoring, summaries, publish)

- Installed `agents-core` from the `v0.1.0` tag (resolves to `b0a292d`, pinned in `uv.lock`); it requires Python 3.12, so `requires-python` is now `>=3.12,<3.13` and ruff targets py312.
- Deleted the temporary `agents/grants/cli.py` standalone dry-run: `uv run agents-run grants --dry-run` (agents-core's runner calling `GrantsAgent.fetch`/`transform`/`summarize_dry_run`) replaces it.
- SAM daily cap is enforced by `agents_core.http`'s `HostPolicy(daily_budget=...)` AND the committed `state.json` ledger; `SamBudget` tightens the Http policy to the ledger so even Http's own retries stop at the smaller limit (agents-core's budget file lives in the uncommitted `.cache/http`, so on a fresh CI checkout the ledger is what binds).
- `max_sam_description_fetches` is treated as a per-UTC-day cap (tracked in `state.sam.description_fetches`), matching its "3 on a 10/day key" framing; descriptions, once fetched (even empty), are stored and never refetched.
- SAM `ptype` is sent as one comma-separated param (`o,k,p,r`); confirmed against the live API (the response had only those types) and locked in by `test_fetch_sam.py`.
- httpx replaces a URL's query when `params=` is given, so `fetch_description` splits the description URL's own query (`noticeid=`) into params; caught by a unit test before any live SAM call.
- SAM content hash is now built only from normalized fields (agency path = agency/sub_agency/office), so `with_sam_description` can recompute it exactly for store-only items; published URL is always the canonical `https://sam.gov/opp/{noticeId}/view` (live `uiLink` points at a workspace URL).
- Grants.gov live shapes differ from the hand-built fixtures: hits carry `cfdaList` (not `alnist`), and detail fields (`applicantTypes`, `awardCeiling`, ...) are nested under `synopsis` or `forecast` with `"none"` strings; `normalize_grants_gov` handles both, and titles are HTML-unescaped (`&amp;` seen live).
- Grants.gov detail cache is a separate `data/grants/grants_gov_details.json.gz` keyed `id|closeDate` (trimmed to the fields normalize reads, pruned to ids still returned by search), so details are fetched once per id+close date even for items later rejected by hard filters.
- Grants relevance now counts `keywords` + `grant_keywords` (reverses the 2026-09-24 call): on live data, 342 of 354 grants scored 0 with `keywords` alone and none could ever reach the 25 threshold, since grants have no NAICS/PSC and the profile has no ALN list.
- `meta.sam_budget_exhausted` / `meta.sam_requests_used` (§6.1): agents-core's runner builds `meta` itself with no extension hook, so `GrantsMeta(RunMeta)` adds the two fields and `GrantsLatest` moves them in from a `sam_meta` body key before validation — the published shape is unchanged; the gap is listed in STATUS.md.
- `sam_requests_used` = the day's ledger count (all SAM requests today, matching the budget's unit), not only this run's.
- "Match" = a scored item recommended Pursue or Consider; `new_since_last_run`, `closing_within_14d`, `active_matches`, `largest_value`, `deadlines_30d` and `items_count` all count matches. `top_matches` is the top 20 by fit regardless of recommendation (§5.5).
- Score guard: `reasons`/`red_flags` are published text, so they go through `fields_guard` against every number in the prompt input (numeric fields + numbers written in its text); a double failure keeps the model's sub-scores and drops only the entries with unsupported numbers (code-built reasons backfill).
- Summary guard = `fields_guard` over all four fields + the §7.3 date check (any `Month D`/`YYYY` in the output must appear in the input; the prompt shows deadlines as "October 15, 2026 4:00 PM EDT") + non-empty `next_steps`.
- Batch timeout (§5.4: 30 min) falls back to sequential sync calls, not concurrency 5 (agents-core's CostTracker isn't documented as thread-safe); a batch whose worst-case estimate exceeds the remaining MAX_RUN_USD is halved until it fits and the rest are deferred to the next run rather than sync-scored at full price.
- A template summary caused by an error or the budget isn't cached (retried next run); one caused by a double guard failure is cached like an LLM summary.
- Eval harness now uses the real scorer/summarizer (proxy removed); eval LLM spend is logged under agent `grants-evals` to `data/evals_costs.jsonl` so it never appears in the published `costs-summary.json`.
- This session's live runs used `--lookback-days=1 --sam-request-budget=3` so that one dry run + two real runs stay within the 3-SAM-request session limit: 1 search (reused from agents-core's HTTP cache by the later runs) + 2 description fetches.
- `agent-grants.yml` passes `cache_path` = `.cache/http` + `public-data` to run-agent.yml's actions/cache step, so agents-core's budget file persists and `history/` actually accumulates (run-agent.yml doesn't check the `data` branch back out before a run).
- Live SAM fixture (`sam_search_live.json`) is recorded from agents-core's HTTP cache by `tools/record_fixtures.py` (no extra SAM request), trimmed to 50 notices, with point-of-contact names/emails/phones redacted.
- After the first live run, speculative red flags ("DoD contract; no clearance held", "requires security clearance likely") were capping fit at 20; the hard-incompatibility cap now needs a stated requirement (requires/must/needed/only) with no hedge (likely/may/unclear/...), and the score prompt (bumped to v2) says to name a clearance only when the input states one.
- Cached scores are re-finalized in code every run (`scoring.refinalize`: sum, caps, bands from the cached sub-scores/red flags), so cap or `[recommendation]` threshold changes apply without LLM calls; only prompt/model/content/profile changes trigger a rescore.
- `SamBudget.get` no longer pre-checks the budget: the ledger-synced Http policy enforces it on network attempts only, so a cached SAM response is still served free after the day's budget is used (found on the session's immediate-rerun check).
- Evals were run twice (the second adds `recommendation_flips` to the output): stability's same-recommendation share was 86.7% (fail) then 96.7% (pass) — flips sit on the 55/75 band edges. Left as is and reported; agents-core exposes no temperature setting to tighten it (listed in STATUS.md).
